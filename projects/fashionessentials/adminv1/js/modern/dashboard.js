import { productsApi, blogsApi } from "../core/api.js";
import { startShell } from "./shell.js";
const money = number => `Rs. ${Number(number || 0).toLocaleString()}`;
async function init(){if(!await startShell("dashboard", `<div class="page-head"><div><h2>Good to see you</h2><p>Here is what is happening in your store today.</p></div><a href="products/product.html" class="btn primary">+ Add product</a></div><div class="stats"><div class="stat"><span>Total products</span><strong id="products">—</strong></div><div class="stat"><span>Active products</span><strong id="active">—</strong></div><div class="stat"><span>Low stock</span><strong id="low">—</strong></div><div class="stat"><span>Published posts</span><strong id="posts">—</strong></div></div><div style="height:22px"></div><div class="card"><div class="toolbar"><div><h3 style="margin:0">Low stock products</h3><p class="hint">Products at or below their low-stock alert level.</p></div><a class="btn" href="products/products.html">View all products</a></div><div style="overflow:auto"><table><thead><tr><th>Product</th><th>SKU</th><th>In stock</th><th>Alert at</th><th>Price</th></tr></thead><tbody id="lowRows"></tbody></table></div></div>`))return;const [products,posts]=await Promise.all([productsApi.list(),blogsApi.list()]);document.getElementById("products").textContent=products.length;document.getElementById("active").textContent=products.filter(p=>p.status==="Active").length;const low=products.filter(p=>Number(p.stock||0)<=Number(p.lowStockAlert??5));document.getElementById("low").textContent=low.length;document.getElementById("posts").textContent=posts.filter(p=>p.status==="Published").length;document.getElementById("lowRows").innerHTML=low.length?low.map(p=>`<tr><td>${p.name}</td><td>${p.sku||"—"}</td><td>${p.stock||0}</td><td>${p.lowStockAlert??5}</td><td>${money(p.salePrice||p.price)}</td></tr>`).join(""):`<tr><td colspan="5" class="empty">No low-stock products. Your inventory is looking healthy.</td></tr>`;}
init().catch(error=>alert(error.message));
